# Deny all direct web access to this folder — it only holds schema.sql,
# which must never be downloadable once the app is live.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
